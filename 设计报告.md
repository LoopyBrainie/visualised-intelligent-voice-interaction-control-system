# 可视化智能语音交互控制系统 — 设计报告

---

## 一、项目背景与实验目的

### 1.1 课程来源

本项目来源于电子信息科学与技术专业 **中级项目课**，课题名称为"可视化智能语音交互控制系统设计与实现"。课程为期 8 周，要求完成一套融合**语音信号采集、指令解析、设备状态控制、可视化建模、多线程并发**的智能家居仿真控制系统。

### 1.2 实验目的

1. 融合电子信息专业核心知识，掌握语音信号采集、指令解析、设备状态控制、可视化建模、多线程并发等工程化开发能力。
2. 熟练掌握 GUI 界面开发、仿真建模、语音识别、手势识别、状态同步、异常处理全流程开发方法，能独立完成完整智能交互控制系统搭建。
3. 培养模块化编程、代码规范、系统联调、问题定位与解决能力，贴近真实工程项目开发流程。
4. 为后续高级课程（单片机、嵌入式开发、物联网通信、人工智能）奠定软件架构与交互控制基础。

### 1.3 项目定位

本项目定位于**纯软件开发 + 可视化交互**的智能家居仿真系统。不涉及真实硬件，通过软件仿真实现语音/手势双模式对虚拟设备（灯、空调、风扇、窗帘）的控制，并实时可视化设备状态变化。

---

## 二、软件总体设计

### 2.1 系统架构总览

系统采用 **三层分离式架构**：GUI 展示层（SolidJS）、业务逻辑层（Rust/Tauri）、数据处理层（Python Flask Daemon）。

```
┌──────────────────────────────────────────────────────────────────────┐
│                      SolidJS Frontend (TSX)                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────────┐  │
│  │ 可视化仿真区   │  │ 音频频谱区     │  │      实时日志区           │  │
│  │ RoomRow +     │  │ SpectrumPanel │  │      LogPanel            │  │
│  │ DeviceCards   │  │ (32柱频谱)    │  │  (timestamp+level+msg)   │  │
│  └──────┬───────┘  └──────┬───────┘  └─────────────┬────────────┘  │
│         └──────────────────┼────────────────────────┘               │
│                     Tauri Events (IPC)                                │
│  spectrum-update / device-state-changed / log_event / vlm-processing │
├─────────────────────────────┼────────────────────────────────────────┤
│                      Rust Backend (src-tauri)                         │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌───────┐ │
│  │ voice.rs │  │control.rs│  │ state.rs │  │logger.rs │  │daemon │ │
│  │ cpal采集 │  │多阶段解析│  │GlobalState│  │事件日志  │  │_mgr.rs│ │
│  │rustfft   │  │strsim    │  │RwLock    │  │LogEntry  │  │健康检查│ │
│  │双轨并行  │  │VLM解析   │  │序列化    │  │          │  │自动恢复│ │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘  └───┬───┘ │
│       │             │             │              │           │       │
│       └─────────────┼─────────────┘              │           │       │
│                     │ HTTP POST /vlm              │           │       │
│                     │ (reqwest)                   │           │ spawn │
├─────────────────────┼────────────────────────────┼───────────┼───────┤
│              Python Daemon (127.0.0.1:8765)       │                   │
│  ┌──────────────────────┐  ┌──────────────────┐   │                   │
│  │ daemon.py (Flask)    │  │ gesture.py       │   │                   │
│  │ /health /shutdown /vlm│  │ (MediaPipe 预留) │   │                   │
│  └──────────┬───────────┘  └──────────────────┘   │                   │
│             │                                       │                   │
│  ┌──────────┴───────────┐                          │                   │
│  │ ai_engine.py         │                          │                   │
│  │ OpenAI SDK → VLM API │                          │                   │
│  │ (MiMo / Kimi / etc.) │                          │                   │
│  └──────────────────────┘                          │                   │
└────────────────────────────────────────────────────┴──────────────────┘
```

**架构设计决策**：原方案采用 PyO3 嵌入式 Python 绑定，实际开发中变更为 HTTP Daemon 进程隔离方案。原因：
- 避免 Python GIL（全局解释器锁）与 Rust 异步运行时的冲突
- 进程隔离提供更好的故障隔离——Python VLM 调用崩溃不影响主进程
- FFT 频谱计算和模糊匹配移至 Rust 原生实现（rustfft + strsim），消除跨语言开销
- HTTP 本地回环延迟 < 5ms，对系统性能无影响

### 2.2 模块划分与职责

| 模块 | 名称 | 主要职责 | 文件分布 |
|------|------|----------|----------|
| **模块 1** | 项目框架 | 脚手架搭建、布局系统、日志底座 | App.tsx, lib.rs, logger.rs |
| **模块 2** | 指令系统 | 指令解析、设备状态管理、命令执行 | control.rs, state.rs |
| **模块 3** | GUI 可视化 | 仿真渲染、设备卡片、液态玻璃 UI、频谱/日志面板 | src/components/ |
| **模块 4** | 语音识别 | 音频采集、FFT 频谱、VLM 集成、进程管理 | voice.rs, daemon_manager.rs, python_engine/ |
| **模块 5** | 手势扩展 | 摄像头手势识别、双模式整合 | gesture.py (预留) |

### 2.3 关键数据结构

#### 2.3.1 设备状态模型（Rust 侧）

```
DeviceState
├── living_room: RoomDeviceState
│   ├── light:          LightState { is_on: bool, brightness: u8 (0-100) }
│   ├── air_condition:  AirConditionState { is_on: bool, temperature: u8 (16-30), mode: ACMode }
│   ├── fan:            FanState { is_on: bool, speed: u8 (0-3) }
│   └── curtain:        CurtainState { is_open: bool }
└── bedroom: RoomDeviceState   (同结构)
```

**线程安全设计**：使用 `Arc<RwLock<DeviceState>>` 封装为 `GlobalState`，支持多读单写。Rust 的所有权系统确保编译期无数据竞争。

#### 2.3.2 前端的响应式镜像（TypeScript 侧）

`deviceStore.ts` 使用 SolidJS `createSignal<DeviceState>` 维护与后端完全一致的状态结构。Tauri `device-state-changed` 事件触发时进行浅合并更新，SolidJS 细粒度响应式系统自动更新所有依赖组件。

### 2.4 前后端通信协议

系统使用 **Tauri 的双向通信机制**：

**前端 → 后端（Commands / IPC 调用）**：
| 命令 | 用途 |
|------|------|
| `handle_voice_command` | 发送文本指令到 Rust 解析执行 |
| `get_device_state` | 启动时同步初始状态 |
| `start_voice_capture` | 启动麦克风采集 + 双轨处理 |
| `stop_voice_capture` | 停止录制 + 发送音频到 VLM |
| `list_audio_devices` | 枚举可用麦克风 |
| `set_language_mode` | 切换语言/手势模式 |

**后端 → 前端（Events / 事件推送）**：
| 事件 | 载荷 | 频率 |
|------|------|------|
| `device-state-changed` | 完整 DeviceState JSON | 每次指令执行后 |
| `spectrum-update` | `{ frequencies: Vec<f32>, sample_rate, fft_size }` | ~60Hz |
| `log_event` | `{ timestamp, level, message, trace_id, source }` | 实时 |
| `vlm-processing` | `bool` | VLM 开始/结束 |
| `env-error` | `string` | 环境初始化失败 |

---

## 三、详细设计与实现

### 3.1 模块 1：项目工程结构与基础框架

#### 3.1.1 技术选型

| 层次 | 技术 | 选型理由 |
|------|------|----------|
| 跨端框架 | **Tauri 2.0** | 相比 Electron 内存更小（< 10MB），原生性能，Rust 安全保证 |
| 前端框架 | **SolidJS** | 无虚拟 DOM 的细粒度响应式，编译时优化，适合高性能实时渲染 |
| 样式系统 | **Tailwind CSS v4** | CSS-first 配置，零运行时，Apple × Morandi 设计系统 |
| 构建工具 | **Vite + Bun** | 极速 HMR，Bun 替代 Node.js 作为包管理器 |
| 包管理 (Python) | **uv** | Rust 编写的极速 Python 包管理器，确定性锁文件 |

#### 3.1.2 布局系统

采用**响应式双列布局**，通过 `useResponsiveLayout` Hook 检测视口宽高比：

- **横屏模式**（宽高比 > 1）：左边可视化仿真区 + 右边频谱/日志面板
- **竖屏模式**（宽高比 ≤ 1）：上中下三段堆叠布局

所有面板使用 `.apple-card` 样式类（白色背景 + 16px 圆角 + 微妙阴影），底层导航栏使用液态玻璃材质。

#### 3.1.3 日志系统

`logger.rs` 维护全局 `AppHandle` 单例，所有日志函数通过 `emit("log_event", LogEntry)` 推送到前端。支持 4 个日志级别（ERROR/WARN/INFO/DEBUG）、链路追踪 ID 和来源标记。Python Daemon 端的日志通过响应的 `logs` 数组字段由 Rust `log_proxy()` 转发。

### 3.2 模块 2：指令系统与设备控制逻辑

#### 3.2.1 六阶段解析流水线

```
原始输入 → ① 归一化（trim, lowercase）
         → ② 房间提取（"卧室" → Bedroom, "客厅" → LivingRoom）
         → ③ 房间前缀剥离（7 种组合尝试）
         → ④ 精确匹配（COMMAND_DICT HashMap, ~28 条）
         → ⑤ 正则数值提取（VALUE_PATTERNS: 温度/亮度/风速）
         → ⑥ 模糊匹配（jaro_winkler, 阈值 0.8）
         → ParsedCommand { cmd_type, target, value, room }
```

#### 3.2.2 命令字典设计

```rust
// 示例条目
COMMAND_DICT = {
    "开灯"     → (TurnOn,  Light),
    "关灯"     → (TurnOff, Light),
    "把灯打开" → (TurnOn,  Light),   // 同一语义多种表达
    ...
}
```

覆盖 4 种设备（灯/空调/风扇/窗帘）、4 种操作（开/关/设值/查询）、2 个房间（客厅/卧室），支持自然语言变体表达。

#### 3.2.3 VLM 响应解析

语音识别流程产生的 JSON 在 Rust 侧解析为 `VlmCommand`：

```
VLM JSON { command: {device, action, room, target_value, confidence} }
  → VlmResponse 反序列化
  → 置信度检查 (confidence > 0.6, 否则 VoiceError::VlmLowConfidence)
  → TryFrom<VlmResponse> for VlmCommand
  → 转换为 ParsedCommand → execute_command → 状态更新
```

#### 3.2.4 模糊匹配算法

采用 **Jaro-Winkler 距离**（Rust `strsim` crate），阈值 0.8：

- Jaro 相似度衡量两个字符串之间匹配字符的比率和换位次数
- Winkler 修正对前缀匹配给予更高权重，适合中文拼音和短指令场景
- 模糊匹配关键词表 `FUZZY_KEYWORDS` 覆盖所有设备/操作变体

### 3.3 模块 3：GUI 界面与可视化建模

#### 3.3.1 设计系统："Apple 的骨架 × Morandi 的肌肤"

以 Apple Human Interface Guidelines 为结构基础，注入莫兰迪色系的温润色调：

| 设计要素 | Apple 侧 | Morandi 侧 |
|----------|----------|------------|
| 主色调 | `#0071e3` 蓝 | — |
| 设备色 | — | 灯光 `#d4a853` 金、风扇 `#7fb572` 绿、空调 `#7da5c4` 蓝、窗帘 `#b8a9c9` 紫 |
| 圆角 | 8-24px 多级 | — |
| 背景 | `#f5f5f7` 浅灰 | `#F2EFE9` 暖白 |
| 材质 | SF Pro 字体 | 液态玻璃 + 微妙阴影 |

#### 3.3.2 组件树

```
App.tsx
├── SnapshotProvider（液态玻璃背景采集上下文）
├── Header（磨砂导航栏，48px）
├── Main（响应式 flex）
│   ├── VisualizationPanel（设备仿真区）
│   │   ├── RoomRow × 2（客厅/卧室，水平滚动）
│   │   │   └── DeviceIconCard × N（SVG 设备图标 + 开关状态）
│   │   └── Portal → LiquidGlass 浮动面板（Light/Fan/AC/Curtain 控件）
│   └── Aside
│       ├── SpectrumPanel（32 柱频谱动画，GPU 加速）
│       └── LogPanel（实时日志，三列布局）
└── ControlBar（底部固定，液态玻璃材质）
    ├── MicButton（圆形录音按钮 + 处理状态动画）
    └── LiquidGlassSegmentToggle（语言/手势模式切换）
```

#### 3.3.3 液态玻璃渲染系统

液态玻璃是本项目最复杂的视觉效果，实现真实的毛玻璃折射效果：

1. **背景截图**：`SnapshotContext` 使用 `modern-screenshot` 的 `domToCanvas()` 在整个应用挂载时拍摄一张不含玻璃层的 DOM 截图，并通过 `MutationObserver`（300ms 防抖）在 DOM 变化时更新
2. **裁剪与位移**：`useLiquidGlass` Hook 将容器所在视口区域的截图裁剪出来
3. **SDF 着色器**：对裁剪图像逐像素应用基于圆角矩形 SDF（Signed Distance Function）的位移——靠近边缘的像素向内偏移，模拟折射效果；远离边缘的像素保持原位（纯透射）
4. **双 Canvas 模糊管线**：位移后的 Canvas → `ctx.filter = 'blur(Npx)'` → 临时 Canvas → 再次 `blur` → 高质感模糊
5. **光照层**：叠加径向渐变 + 内阴影（`mix-blend-mode: overlay`），营造玻璃厚度感
6. **ErrorBoundary 降级**：Canvas 操作失败时自动回退到 CSS `backdrop-filter: blur()` 方案

#### 3.3.4 设备图标系统

所有设备图标使用**内联 SVG**，无外部资源依赖。每个图标根据设备状态动态切换颜色：

- **关闭态**：Morandi 灰色系，扁平背景
- **开启态**：Morandi 彩色 + 60% 不透明度背景 + Apple 卡片阴影
- **灯光特殊效果**：`drop-shadow(0 0 6px #c9a86c)` 发光
- **风扇动画**：CSS `fanSpin` 旋转动画（开启时触发）

### 3.4 模块 4：语音识别与多线程并发

#### 3.4.1 双轨并行音频处理架构

这是模块 4 最核心的设计——单次音频采集同时服务两个目标：

```
cpal 音频回调（48kHz, WASAPI 共享模式, I16/F32/U16 多格式兼容）
    │
    ├──→ [实时轨道] mpsc::sync_channel → 独立线程
    │       │
    │       └── FftProcessor::process(samples)
    │           ├── 512 点 FFT (rustfft)
    │           ├── Hann 窗口函数: w[n] = 0.5 - 0.5·cos(2πn/511)
    │           ├── 幅度谱 → dB 尺度归一化: 20·log₁₀(|X[k]|+1)/60
    │           ├── 50% 重叠 (256 样本步进)
    │           └── emit("spectrum-update") @ ~60Hz
    │
    └──→ [指令轨道] RecordingState 缓冲区 → tokio 异步任务
            │
            ├── 用户点击开始 → start_recording() → 累积音频
            ├── 用户点击停止 → stop_recording_and_send()
            │     └── VlmMessage::Stop → mpsc channel
            │           └── 提取缓冲区全部 i16 样本
            │           └── VlmClient::send_audio()
            │                 ├── PCM i16 → 小端字节 → Base64
            │                 ├── reqwest HTTP POST → 127.0.0.1:8765/vlm
            │                 ├── 15s timeout (tokio::time::timeout)
            │                 └── 响应 JSON 解析
            └── parse_vlm_response() + execute_vlm_command()
                  └── emit("device-state-changed")
```

#### 3.4.2 FFT 数字信号处理

**窗函数选择**：Hann 窗相比矩形窗具有更好的旁瓣抑制（-31.5 dB/oct），适合音频频谱分析。

**数学表示**：
- FFT：$X[k] = \sum_{n=0}^{511} x[n] \cdot w[n] \cdot e^{-j\frac{2\pi}{512}nk}$
- 幅度：$|X[k]| = \sqrt{\text{Re}(X[k])^2 + \text{Im}(X[k])^2}$
- dB 归一化：$Y[k] = \min(1, \max(0, \frac{20\log_{10}(|X[k]| + 1)}{60}))$

**前端压缩**：512 点频谱通过 `lib/spectrum.ts` 压缩为 32 个频带（对数尺度分组取均值），每个柱的 `transform: scaleY(N)` 通过 GPU 合成层渲染，0.15s cubic-bezier 过渡。

#### 3.4.3 VLM 集成管线

```
用户语音 → cpal PCM 48kHz i16 样本
  → base64 编码
  → reqwest POST → Flask Daemon (:8765/vlm)
  → ai_engine.py: PCM 字节 → wave 模块编码为 WAV → base64
  → OpenAI SDK → VLM API (MiMo v2.5 / Kimi / 任意 OpenAI 兼容)
  → 系统提示词定义设备/动作/房间/数值范围
  → JSON 响应 { device, action, room, target_value, confidence }
  → Python 端验证结构 → 返回 Rust
  → control.rs: 置信度 > 0.6 → execute_vlm_command
  → 设备状态更新 → emit("device-state-changed")
```

**系统提示词设计**：指示 VLM 扮演智能家居语音指令解析器，明确：
- 设备集：`light, fan, air_condition, curtain`
- 动作集：`on, off, set_level, auto, stop, open, close`
- 数值范围：灯光 0-100、空调 16-30、风扇 1-3
- 输出格式：固定 JSON 结构
- 置信度阈值：0.6（低于此值的指令在 Rust 侧拒绝执行）

#### 3.4.4 HTTP Daemon 进程管理

`DaemonManager`（`daemon_manager.rs`）负责 Python Flask 进程的完整生命周期：

- **启动**：`resolve_python_runtime()` 定位 uv 虚拟环境中的 Python，spawn `daemon.py`
- **健康检查**：轮询 `GET /health`，最多等待 5 秒（100ms 间隔）
- **运行时监控**：`ensure_running()` 检测进程存活，宕机自动重启
- **优雅关闭**：`POST /shutdown` → 500ms 等待 → 强制 SIGKILL
- **Drop 清理**：析构时自动停止子进程，防止孤儿进程

### 3.5 模块 5：手势扩展（进行中）

#### 3.5.1 预留框架

手势识别模块已搭建**预留框架**：

- `python_engine/gesture.py`：MediaPipe Hands 初始化、`count_fingers()` 和 `recognize_gesture()` 函数签名
- 前端模式切换：`ControlBar` → `LiquidGlassSegmentToggle` → `set_language_mode(true/false)`
- 手势模式下暂停音频流 FFT（`stream_paused = true`），节省电量

#### 3.5.2 计划的手势映射

| 手势 | 手指数量 | 映射动作 |
|------|----------|----------|
| 手掌张开 | 5 | 打开所有设备 |
| 拳头 | 0 | 关闭所有设备 |
| 比耶 | 2 | 自定义动作 |
| 单指 | 1 | 选择特定设备 |

#### 3.5.3 声纹识别扩展预留

`python_engine/voiceprint.py` 已预留 MFCC 特征提取和欧氏距离校验接口，可在后续阶段快速集成。

---

## 四、关键技术实现

### 4.1 数字信号处理（Rust 原生）

**为什么在 Rust 侧做 FFT 而非 Python（scipy）？**
- 零跨语言开销，FFT 计算在音频回调线程中同步完成
- rustfft 是 SIMD 优化的纯 Rust 实现，性能接近 FFTW
- 避免 Python GIL 阻塞音频采集回调

**核心实现**（voice.rs `FftProcessor`）：
- FFT 大小：512 点（2 的幂次，最优 radix-2 分解）
- 窗口：Hann 窗，重叠 50%（256 样本步进）
- 输出归一化：dB 尺度，Clamp 到 [0, 1]，直接映射到前端 32 柱高度

### 4.2 VLM 语音指令识别

**为什么用 VLM 而非传统 ASR + NLP？**
- VLM 原生支持多模态输入（音频 + 文本提示词），无需中间 ASR 步骤
- 通过系统提示词即可精确控制输出格式（JSON schema）
- 模型选择灵活——支持 MiMo、Kimi、GPT-4o 等任何 OpenAI 兼容 API

**音频编码**：PCM i16 → Python `wave` 模块封装为 WAV 格式（RIFF 头 + 样本数据），VLM API 需要 WAV 容器而非裸 PCM。

### 4.3 液态玻璃渲染

液态玻璃效果的实现是项目中最复杂的纯前端技术：

1. **SDF（Signed Distance Function）着色器**（`shaderUtils.ts`）：计算每个像素到圆角矩形边界的符号距离，在 `smoothStep` 过渡区内产生向内折射可见的位移
2. **像素级操作**：通过 Canvas 2D `getImageData()` / `putImageData()` 逐像素修改，实现 `useLiquidGlass` 的 `fragment()` 函数
3. **性能考量**：液态玻璃容器在面板打开时通过 `SnapshotContext.notifyPanelOpen()` 冻结背景截图，避免递归渲染；尺寸变化时重新计算
4. **降级策略**：Canvas 模式失败时回退到 SVG `feDisplacementMap` 滤镜模式，再失败则回退到 CSS `backdrop-filter`

### 4.4 进程管理与容错

**多进程架构的容错设计**：
- Python Daemon 崩溃 → `ensure_running()` 自动重启（health check 驱动）
- VLM API 调用失败 → `ai_engine.py` 指数退避重试（0.5s / 1s / 2s，最多 3 次）
- Rust 主进程 → 不受 Python 子进程状态影响，GUI 始终可用

---

## 五、异常处理与容错机制

| 异常场景 | 检测方式 | 处理策略 | 用户感知 |
|----------|----------|----------|----------|
| 麦克风不可用 | `cpal::default_input_device()` 返回 None | 日志 ERROR + `list_audio_devices` 返回空列表 | 麦克风按钮无响应，日志提示 |
| Python 进程启动失败 | DaemonManager health check 超时（5s） | 日志 ERROR，GUI 继续运行 | 语音控制不可用，手动控制正常 |
| Python 进程运行时崩溃 | `ensure_running()` 健康检查失败 | 自动重启子进程 | 短暂不可用后自动恢复 |
| VLM 请求超时 | `tokio::time::timeout(15s)` | `VoiceError::VlmTimeout` 日志输出 | 日志显示"VLM 请求超时"，重新就绪 |
| VLM 置信度过低 | `confidence < 0.6` | `VoiceError::VlmLowConfidence` 拒绝执行 | 日志输出置信度数值，不触发误操作 |
| VLM 响应格式异常 | `serde_json::from_str` 失败 | `VoiceError::VlmParseError` | 日志输出原始响应和错误信息 |
| 网络异常（VLM API） | `reqwest::Error` | 指数退避重试 3 次 | 重试后仍失败显示错误日志 |
| 指令无法匹配 | 精确匹配 + 正则 + 模糊匹配均失败 | `ControlError::Unrecognized` | 日志显示未识别指令原文 |

---

## 六、测试与验证

### 6.1 测试策略

| 测试层次 | 工具 | 覆盖范围 |
|----------|------|----------|
| Rust 单元测试 | `cargo test` | control.rs ~80 测试用例、state.rs 5 测试用例 |
| 前端组件测试 | 手动验证 (Tauri dev) | 所有组件渲染、交互、状态同步 |
| 集成测试 | 端到端语音指令流程 | 麦克风 → FFT → VLM → 指令执行 → UI 更新 |

### 6.2 验收标准达成

| 模块 | 验收项数量 | 已完成 | 状态 |
|------|-----------|--------|------|
| 模块 1：基础框架 | 5 | 5 | ✅ 完成 |
| 模块 2：指令系统 | 6 | 6 | ✅ 完成 |
| 模块 3：GUI 界面 | 6 | 6 | ✅ 完成 |
| 模块 4：语音识别 | 7 | 7 | ✅ 完成 |
| 模块 5：手势扩展 | 4 | 0 | ⏳ 进行中 |

### 6.3 性能指标

| 指标 | 目标 | 实际 | 说明 |
|------|------|------|------|
| FFT 延迟 | < 50ms | ~16ms | 60Hz 事件发射频率 |
| 界面刷新 | ≥ 60fps | ≥ 30fps (录音时) | SolidJS 细粒度更新 |
| 前后端通信 | < 100ms | < 10ms | Tauri IPC 原生性能 |
| HTTP Daemon 延迟 | — | < 5ms | localhost 回环 |
| VLM 识别延迟 | — | 2-8s | 取决于 VLM API 响应 |
| 内存占用 | — | ~50MB (含 Python) | Tauri 轻量优势 |

---

## 七、系统部署与运行

### 7.1 开发环境

| 工具 | 版本 | 用途 |
|------|------|------|
| Rust | 1.85+ (MSVC) | 后端编译 |
| Python | 3.12 | VLM 引擎 + 预留模块 |
| uv | latest | Python 包管理 |
| Bun | latest | 前端包管理 + 脚本 |
| Windows 10/11 | — | 目标平台 (Tauri WASAPI) |

### 7.2 启动流程

```
1. cd python_engine && uv sync    # 首次：创建 venv 并安装依赖
2. cd .. && bun install            # 首次：安装前端依赖
3. bun tauri dev                   # 启动 Vite + Tauri 开发模式
   ├── Vite dev server (:5173)     # 前端 HMR
   ├── cargo build                 # Rust 编译
   ├── Python 环境解析             # lib.rs ensure_python_env()
   ├── Python Daemon 启动          # daemon_manager.rs (:8765)
   └── Tauri 窗口打开 (800×600)    # GUI 就绪
```

### 7.3 关键配置

- **VLM API**：`python_engine/vlm_config.toml` — base_url、api_key、model
- **Tauri**：`src-tauri/tauri.conf.json` — 窗口尺寸 800×600、devUrl localhost:3000
- **Tailwind**：`src/styles/app.css` — CSS-first `@theme` 配置

---

## 八、结论与展望

### 8.1 功能完成度

| 功能 | 状态 | 备注 |
|------|------|------|
| 基础框架 + GUI 布局 | ✅ | 响应式双列布局 + 液态玻璃 UI |
| 指令系统 + 设备控制 | ✅ | 6 阶段解析 + ~28 条命令字典 + 模糊匹配 |
| GUI 可视化建模 | ✅ | 4 设备 SVG + 浮动控制面板 + 32 柱频谱 |
| 语音采集 + FFT | ✅ | cpal 48kHz + rustfft 512pt + 双轨并行 |
| VLM 语音识别 | ✅ | HTTP Daemon + OpenAI SDK 管线 |
| 实时日志 + 链路追踪 | ✅ | trace_id 全链路贯穿 |
| 异常处理 + 容错 | ✅ | 8 类异常场景全覆盖 |
| 手势识别 | ⏳ | MediaPipe 框架已预留，待实现 |
| 声纹识别 | ⏳ | MFCC 接口已预留 |
| 定时控制 | ⏳ | 可扩展 |

### 8.2 技术亮点

1. **双轨并行音频架构**：单次麦克风采集同时服务实时频谱（60Hz）和指令识别，互不阻塞
2. **液态玻璃 UI 系统**：基于 SDF 着色器的逐像素折射 + 双 Canvas 模糊管线 + ErrorBoundary 降级
3. **Rust 全栈 DSP**：FFT 和模糊匹配在 Rust 侧原生实现，零跨语言开销
4. **多进程容错设计**：Python 子进程崩溃不影响主进程，自动重启 + 健康检查
5. **全链路追踪**：trace_id 贯穿 Rust → Python → Rust → 前端日志，便于调试

### 8.3 不足与改进方向

1. **手势识别未实现**：gesture.py 为存根，需实现 MediaPipe 手部关键点检测和手指计数逻辑
2. **声纹识别**：voiceprint.py 接口已预留，需采集声纹样本和调优 MFCC 阈值
3. **VLM 延迟**：云端 VLM 调用 2-8s 的延迟影响交互体验，可考虑本地 Whisper 模型
4. **WASAPI 独占模式**：当前共享模式需要 VLM 端 48→16kHz 重采样，可改进为请求式重采样
5. **Dark Mode**：主题变量已定义但未实现自动切换
6. **自动化测试**：前端组件缺少自动化测试（vitest + testing-library）

### 8.4 课程收获

通过本项目的完整开发，实践了以下电子信息专业核心能力：
- **信号处理**：FIR 滤波、FFT 频谱分析、Hann 窗函数的工程应用
- **多线程编程**：Rust Tokio 异步 + mpsc channel 的并发架构设计
- **系统集成**：Rust/Python/TypeScript 三语言协作的跨进程通信
- **UI 工程化**：Canvas 2D 像素操作、SDF 着色器、GPU 加速动画
- **工程规范**：模块化设计、错误处理链、进度追踪、测试覆盖

---

## 附录：完整文件清单

| 文件路径 | 行数 | 职责 |
|----------|------|------|
| `src/App.tsx` | ~97 | 根组件，响应式布局编排 |
| `src/store/deviceStore.ts` | ~85 | 前端响应式状态管理 |
| `src/components/VisualizationPanel.tsx` | ~162 | 房间设备仿真区 |
| `src/components/SpectrumPanel.tsx` | ~83 | 32 柱频谱动画 |
| `src/components/LogPanel.tsx` | ~137 | 实时日志终端 |
| `src/components/ControlBar.tsx` | ~66 | 底部控制栏 |
| `src/components/ui/LiquidGlass.tsx` | ~129 | 液态玻璃容器 |
| `src/hooks/useLiquidGlass.ts` | ~326 | 液态玻璃核心 Hook |
| `src/hooks/useResponsiveLayout.ts` | ~20 | 响应式布局检测 |
| `src/lib/spectrum.ts` | ~137 | FFT 事件订阅 |
| `src/lib/shaderUtils.ts` | ~53 | SDF 片段着色器 |
| `src/styles/app.css` | ~716 | 设计系统 + 组件样式 |
| `src-tauri/src/lib.rs` | ~416 | Tauri 命令 + Python 环境 |
| `src-tauri/src/voice.rs` | ~669 | 音频采集 + FFT + VLM 客户端 |
| `src-tauri/src/control.rs` | ~1139 | 指令解析 + VLM 命令执行 |
| `src-tauri/src/state.rs` | ~270 | 设备状态 + 线程安全 |
| `src-tauri/src/logger.rs` | ~161 | 日志事件发射 |
| `src-tauri/src/daemon_manager.rs` | ~206 | Python 进程管理 |
| `python_engine/daemon.py` | ~215 | Flask HTTP 服务 |
| `python_engine/ai_engine.py` | ~360 | VLM API 调用 |
| `python_engine/gesture.py` | ~23 | 手势识别存根 |
| `python_engine/voiceprint.py` | ~20 | 声纹识别预留接口 |
